'use client';
import { BarChart3, Database, Group, LogOut, Search, ShieldCheck } from 'lucide-react';
import { useState } from 'react';

type Tab='entries'|'groups'|'users';
export default function Admin(){const [tab,setTab]=useState<Tab>('entries'); const [query,setQuery]=useState(''); return <main className="min-h-screen bg-[var(--cream)]"><header className="border-b border-[var(--ink)]/10 bg-white/50"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-[var(--ink)] text-[var(--gold)]"><ShieldCheck size={17}/></div><div><div className="eyebrow text-[var(--coral)]">Admin workspace</div><div className="font-semibold">The common thread</div></div></div><button onClick={()=>window.location.href='/'} className="flex items-center gap-2 text-sm text-[var(--ink)]/60"><LogOut size={16}/> Back to site</button></div></header><div className="mx-auto max-w-7xl px-6 py-10"><div className="mb-10 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="eyebrow text-[var(--coral)]">Keep the ledger true</div><h1 className="mt-2 text-5xl">Admin dashboard</h1><p className="mt-3 text-[var(--ink)]/60">Your live workspace for the ward service ledger.</p></div><button onClick={()=>window.location.href='/#submit'} className="rounded-full bg-[var(--ink)] px-5 py-3 text-sm font-bold text-white">+ Add entry</button></div><div className="mb-8 grid gap-4 md:grid-cols-3"><Metric icon={<BarChart3 size={18}/>} label="Total hours" value="0.0" note="No service yet"/><Metric icon={<Database size={18}/>} label="Entries" value="0" note="Fresh ledger"/><Metric icon={<Group size={18}/>} label="Active groups" value="0" note="No groups yet"/></div><div className="card overflow-hidden rounded-3xl"><div className="flex flex-col gap-4 border-b border-[var(--ink)]/10 px-6 py-5 md:flex-row md:items-center md:justify-between"><div className="flex items-center gap-3"><TabButton active={tab==='entries'} onClick={()=>setTab('entries')}>All entries</TabButton><TabButton active={tab==='groups'} onClick={()=>setTab('groups')}>Groups</TabButton><TabButton active={tab==='users'} onClick={()=>setTab('users')}>Users</TabButton></div>{tab==='entries'&&<label className="flex items-center gap-2 rounded-full border border-[var(--ink)]/10 bg-white/60 px-4 py-2 text-sm text-[var(--ink)]/50"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} className="w-44 bg-transparent outline-none" placeholder="Search entries..."/></label>}</div>{tab==='entries'&&<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-[var(--ink)]/[.03] text-[var(--ink)]/45"><tr><th className="px-6 py-4 font-medium">Entity</th><th className="px-6 py-4 font-medium">Service</th><th className="px-6 py-4 font-medium">Date</th><th className="px-6 py-4 text-right font-medium">Hours</th></tr></thead><tbody><tr><td colSpan={4} className="px-6 py-16 text-center text-[var(--ink)]/50">{query?`No entries match “${query}”.`:'No entries yet. Add the first contribution from the public form.'}</td></tr></tbody></table></div>}{tab==='groups'&&<Empty title="No groups yet" copy="Your group list is empty. Add the ward’s groups when you’re ready." action="Add a group"/>}{tab==='users'&&<Empty title="No admin users listed" copy="Admin access will appear here once authentication is configured." action="Manage access"/>}</div></div></main>}
function TabButton({active,onClick,children}:{active:boolean;onClick:()=>void;children:React.ReactNode}){return <button onClick={onClick} className={`rounded-full px-4 py-2 text-xs font-bold ${active?'bg-[var(--ink)] text-white':'text-[var(--ink)]/50 hover:bg-[var(--mist)]'}`}>{children}</button>}
function Empty({title,copy,action}:{title:string;copy:string;action:string}){return <div className="px-6 py-16 text-center"><h2 className="text-2xl">{title}</h2><p className="mx-auto mt-3 max-w-md text-sm text-[var(--ink)]/55">{copy}</p><button className="mt-6 rounded-full bg-[var(--ink)] px-5 py-3 text-sm font-bold text-white">+ {action}</button></div>}
function Metric({icon,label,value,note}:{icon:React.ReactNode;label:string;value:string;note:string}){return <div className="card rounded-2xl p-5"><div className="flex items-center gap-2 text-[var(--coral)]">{icon}<span className="eyebrow text-[var(--ink)]/50">{label}</span></div><div className="mt-4 font-mono text-3xl">{value}</div><div className="mt-2 text-xs text-[var(--ink)]/45">{note}</div></div>}
