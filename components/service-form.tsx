'use client';
import { useState } from 'react';
export function ServiceForm(){const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setStatus('');const body=new FormData(e.currentTarget);const res=await fetch('/api/service-entries',{method:'POST',body});if(res.ok){window.location.assign('/');return}setBusy(false);setStatus('Could not save that entry. Please try again.')}return <form onSubmit={submit} className="card rounded-3xl bg-[var(--cream)] p-6 md:p-8"><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold">Your name or group<input name="entity_name" required className="mt-2 w-full rounded-xl border border-[var(--ink)]/15 bg-white/60 px-4 py-3 outline-none focus:border-[var(--coral)]" placeholder="e.g. The Johnson family"/></label><label className="text-sm font-semibold">Hours served<input name="hours" required type="number" min=".1" step=".1" className="mt-2 w-full rounded-xl border border-[var(--ink)]/15 bg-white/60 px-4 py-3 outline-none focus:border-[var(--coral)]" placeholder="0.0"/></label></div><label className="mt-5 block text-sm font-semibold">What did you do?<textarea name="description" required rows={3} className="mt-2 w-full resize-none rounded-xl border border-[var(--ink)]/15 bg-white/60 px-4 py-3 outline-none focus:border-[var(--coral)]" placeholder="Tell us a little about the service..."/></label><div className="mt-5 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><label className="text-sm font-semibold">Date<input name="service_date" required type="date" className="ml-3 rounded-lg border border-[var(--ink)]/15 bg-white/60 px-3 py-2 text-sm"/></label><div className="flex items-center gap-4"><span className="text-sm text-[var(--ink)]/60">{status}</span><button disabled={busy} type="submit" className="rounded-full bg-[var(--ink)] px-7 py-3 text-sm font-bold text-white transition hover:bg-[var(--coral)] disabled:opacity-50">{busy?'Saving…':'Add service →'}</button></div></div></form>}
